<!--
Purpose:
This file is generated by scripts/generate-compose-docs.js.
Do not edit by hand. Update docker-compose.yml or config/services.yml, then run:

  pnpm docs:compose
-->

# Docker Compose Architecture

Generated from: `docker-compose.yml`

---
## Compose Profiles

Profiles allow progressive startup — only start what you need.

| Profile | # Services | Description | Service list |
| --- | --- | --- | --- |
| default | 4 | Starts infrastructure only — postgres, redis, rabbitmq, minio. | postgres, redis, rabbitmq, minio |
| core | 6 | Starts the minimum backend needed for auth, courses, enrollment, and API routing. | nginx, gateway, auth-service, user-service, course-service, enrollment-service |
| learn | 2 | Starts learning assessment services — quizzes and assignments. | quiz-service, assignment-service |
| finance | 2 | Starts payment and wallet services. Financial logic is isolated. | wallet-service, payment-service |
| ops | 4 | Starts operational support services — notifications, media, certificates, analytics. | notification-service, media-service, certificate-service, analytics-service |
| frontend | 1 | Starts the Next.js web application. Requires core profile for nginx. | web |
| ai | 2 | Starts local AI inference and the AI service. Requires core profile. | ollama, ai-service |

### Quick-start commands

```bash
# Infrastructure only — postgres, redis, rabbitmq, minio  (~1.2 GB)
docker compose up -d

# Core backend — adds nginx, gateway, auth, course, enrollment  (~2.0 GB)
docker compose --profile core up -d

# Full backend — adds quiz, assignment, wallet, payment, notification, media, certificate, analytics  (~3.7 GB)
docker compose --profile core --profile learn --profile finance --profile ops up -d

# Full stack — adds web frontend  (~4.0 GB)
docker compose --profile core --profile learn --profile finance --profile ops --profile frontend up -d

# Add AI inference to any of the above  (~+4.5 GB)
docker compose --profile core --profile ai up -d
```

---

## YAML Anchors Reference

Anchors are defined at the top of `docker-compose.yml` using the `x-*` extension key convention.
The YAML parser expands them inline before Docker Compose processes the file.

> **Why a single compose file?**
> YAML anchors cannot cross file boundaries. An anchor defined in `docker-compose.infra.yml`
> cannot be referenced in `docker-compose.core.yml`. Splitting into per-profile files would
> require duplicating every anchor in every file — which is worse than a single 600-line file.
> The profile system already gives you selective startup without file splitting.

| Anchor | Expands to | Used by |
| --- | --- | --- |
| *restart | restart: unless-stopped | All services via *nestjs-common |
| *networks | networks: [lms-net] | All services via *nestjs-common |
| *nestjs-build | build.context + build.target: runner | All built NestJS services |
| *nestjs-healthcheck | interval: 30s / timeout: 10s / 3 retries / 40s start | All NestJS services |
| *nestjs-resources | deploy.resources.limits.memory: 192M | Standard NestJS services via *nestjs-common |
| *nestjs-common | restart + networks + nestjs-resources + env_file | All standard NestJS services |
| *nestjs-env | NODE_OPTIONS, NODE_ENV | Every NestJS environment block |
| *minio-env | 6 shared MinIO connection vars (endpoint, port, ssl, keys, public URL) | media-service, certificate-service |
| *heavy-common | restart + networks + env_file + 320M | media-service |
| *infra-depends | postgres + redis + rabbitmq all service_healthy | All domain NestJS services |

---

## Memory Tiers

Memory limits are set by compose anchors and deploy overrides, not by the services themselves.

### Tier overview

| Tier | Limit | Services |
| --- | --- | --- |
| AI inference | 4096M | ollama |
| AI service | 384M | ai-service |
| Heavy (file I/O) | 320M | media-service |
| Infrastructure | 256–384M | postgres (384M), redis (256M), rabbitmq (384M) |
| Web frontend | 256M | web |
| Standard NestJS | 192M | all other domain services |
| nginx | 64M | nginx |

### Per-service limits

| Service | Profile | Memory |
| --- | --- | --- |
| ollama | ai | 4096M |
| postgres | default | 384M |
| rabbitmq | default | 384M |
| ai-service | ai | 384M |
| media-service | ops | 320M |
| redis | default | 256M |
| web | frontend | 256M |
| minio | default | 192M |
| gateway | core | 192M |
| auth-service | core | 192M |
| user-service | core | 192M |
| course-service | core | 192M |
| enrollment-service | core | 192M |
| quiz-service | learn | 192M |
| assignment-service | learn | 192M |
| wallet-service | finance | 192M |
| payment-service | finance | 192M |
| notification-service | ops | 192M |
| certificate-service | ops | 192M |
| analytics-service | ops | 192M |
| nginx | core | 64M |

---

## Port Allocations and Binding Rules

### Rule: localhost-only by default

Every service except `gateway` and `nginx` binds to `127.0.0.1`.
This means service ports are not reachable from outside the host in development.
All external traffic enters through nginx (port 80), which proxies to gateway (port 3000).

`gateway` binds to `0.0.0.0` so host browsers can reach the API directly during development.
This is intentional — do not change it to `127.0.0.1` without considering the dev workflow.

| Service | Profile | Host Binding | Port Mapping |
| --- | --- | --- | --- |
| postgres | default | 127.0.0.1 (localhost) | 127.0.0.1:5432:5432 |
| redis | default | 127.0.0.1 (localhost) | 127.0.0.1:6379:6379 |
| rabbitmq | default | 127.0.0.1 (localhost) | 127.0.0.1:5672:5672, 127.0.0.1:15672:15672 |
| minio | default | 127.0.0.1 (localhost) | 127.0.0.1:9000:9000, 127.0.0.1:9001:9001 |
| nginx | core | 0.0.0.0 (public) | 80:80 |
| gateway | core | 0.0.0.0 (public) | 3000:3000 |
| auth-service | core | 127.0.0.1 (localhost) | 127.0.0.1:3001:3001 |
| user-service | core | 127.0.0.1 (localhost) | 127.0.0.1:3014:3014 |
| course-service | core | 127.0.0.1 (localhost) | 127.0.0.1:3003:3003 |
| enrollment-service | core | 127.0.0.1 (localhost) | 127.0.0.1:3004:3004 |
| quiz-service | learn | 127.0.0.1 (localhost) | 127.0.0.1:3005:3005 |
| assignment-service | learn | 127.0.0.1 (localhost) | 127.0.0.1:3006:3006 |
| wallet-service | finance | 127.0.0.1 (localhost) | 127.0.0.1:3007:3007 |
| payment-service | finance | 127.0.0.1 (localhost) | 127.0.0.1:3008:3008 |
| notification-service | ops | 127.0.0.1 (localhost) | 127.0.0.1:3010:3010 |
| media-service | ops | 127.0.0.1 (localhost) | 127.0.0.1:3011:3011 |
| certificate-service | ops | 127.0.0.1 (localhost) | 127.0.0.1:3012:3012 |
| analytics-service | ops | 127.0.0.1 (localhost) | 127.0.0.1:3013:3013 |
| ollama | ai | 127.0.0.1 (localhost) | 127.0.0.1:11434:11434 |
| ai-service | ai | 127.0.0.1 (localhost) | 127.0.0.1:3009:3009 |

---

## Health Check Conventions

Every NestJS service must expose `GET /api/health` returning HTTP 200.
The healthcheck start period (40s) covers Prisma client init + NestJS bootstrap (~10–20s).

**Two health check commands:**

- `curl -f` — used by all NestJS services (curl is present in the production image)
- `wget -qO-` — used by `gateway` and `nginx` (Alpine base images prefer wget)

| Service | Command | Interval | Start Period |
| --- | --- | --- | --- |
| postgres | pg_isready -U ${POSTGRES_USER:-lms} -d postgres | 10s | 20s |
| redis | redis-cli ping | 10s | 10s |
| rabbitmq | rabbitmq-diagnostics ping | 15s | 30s |
| minio | mc ready local | 20s | 20s |
| nginx | wget -qO- http://127.0.0.1:80/api/health | 30s | 15s |
| gateway | wget -qO- http://127.0.0.1:3000/api/health \|\| exit 1 | 30s | 30s |
| auth-service | curl -f http://127.0.0.1:3001/api/health | 30s | 40s |
| user-service | curl -f http://127.0.0.1:3014/api/health | 30s | 40s |
| course-service | curl -f http://127.0.0.1:3003/api/health | 30s | 40s |
| enrollment-service | curl -f http://127.0.0.1:3004/api/health | 30s | 40s |
| quiz-service | curl -f http://127.0.0.1:3005/api/health | 30s | 40s |
| assignment-service | curl -f http://127.0.0.1:3006/api/health | 30s | 40s |
| wallet-service | curl -f http://127.0.0.1:3007/api/health | 30s | 40s |
| payment-service | curl -f http://127.0.0.1:3008/api/health | 30s | 40s |
| notification-service | curl -f http://127.0.0.1:3010/api/health | 30s | 40s |
| media-service | curl -f http://127.0.0.1:3011/api/health | 30s | 40s |
| certificate-service | curl -f http://127.0.0.1:3012/api/health | 30s | 40s |
| analytics-service | curl -f http://127.0.0.1:3013/api/health | 30s | 40s |
| web | wget -qO- http://127.0.0.1:3002/ \|\| exit 1 | 30s | 30s |
| ollama | curl -f http://127.0.0.1:11434/api/tags | 30s | 60s |
| ai-service | curl -f http://127.0.0.1:3009/api/health | 30s | 40s |

---

## Service Grouping Strategy

All profiles share the same `lms-net` Docker network.
Any running service can reach any other running service by its compose service key (Docker DNS).

### Profile dependency hierarchy

```
default (infra: postgres, redis, rabbitmq, minio)
  └── core     (nginx, gateway, auth-service, course-service, enrollment-service)
        ├── learn    (quiz-service, assignment-service)
        ├── finance  (wallet-service, payment-service)
        ├── ops      (notification-service, media-service, certificate-service, analytics-service)
        ├── frontend (web)
        └── ai       (ollama, ai-service)
```

Compose does not enforce this hierarchy — starting `learn` without `core` will fail at runtime
because services cannot find their dependencies. Always start profiles bottom-up.

### Adding a new service

1. Add an entry to `config/services.yml` (set `status: reserved` if compose is not ready yet).
2. Add a service block to `docker-compose.yml` — use `*nestjs-common` and `*infra-depends`.
3. Use the next available port (3015+). Ports 3001–3013 are allocated; 3014 is reserved for user-service.
4. Bind to `127.0.0.1` — not `0.0.0.0` — unless the service needs public access.
5. Implement `GET /api/health` returning HTTP 200.
6. Add a dev entry in `docker-compose.override.yml` following the existing pattern.
7. Run `pnpm docs:generate && pnpm docs:compose` and commit the updated docs.
8. Run `pnpm verify:compose && pnpm docs:verify:services` to confirm no regressions.
