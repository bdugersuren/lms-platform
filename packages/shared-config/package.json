{
  "name": "@lms/shared-config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared NestJS configuration factories with Joi validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nestjs/config": "^3.2.3",
    "joi": "^17.13.1"
  },
  "devDependencies": {
    "@nestjs/common": "^10.3.10",
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  }
}
